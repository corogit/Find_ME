<div class="container">
  <div class="row mt-5">
    <div class="col-lg-3">
       <h2>ペット詳細</h2>
      <p><%= attachment_image_tag @pet, :image, size: "350x350", format: 'jpg', fallback: "no_image.jpg", class: "rounded mt-4" %></p>
    </div>

    <div class="col-lg-8 offset-lg-1">
      <div class="pet.name mt-5 ml-5">
        <p>ジャンル: <%= @pet.genre.name %></p>
        <p>お名前: <%= @pet.name %></p>
        <p>お誕生日: <%= @pet.birthday %></p>
        <p>年齢: <%= @pet.age %>才</p>
        <p>性別: <%= @pet.gender %></p>
        <p>都道府県: <%= @pet.prefecture.name %></p>
        <p>特徴: <%= @pet.introduction %></p>
        <p>ステータス: 
          <% if @pet.is_active == true %>
            飼い主さん募集中
          <% elsif @pet.is_active == false %>
            新しいおうちが見つかったよ！
          <% end %></p>
        <p>投稿者:
        <% if user_signed_in? %>
          <%= link_to user_path(@pet.user) do %>
            <%= attachment_image_tag @pet.user, :profile_image, size: "60x60", format: 'jpeg', fallback: "no_image_icon.jpg", class: "rounded-circle" %>
          <% end %>
        <% else %>
          <%= attachment_image_tag @pet.user, :profile_image, size: "60x60", format: 'jpeg', fallback: "no_image_icon.jpg", class: "rounded-circle" %>
        <% end %>
        </p>
        <% if user_signed_in? %>
        <div class="favorite-btn<%= @pet.id %>">
          <%= render 'favorites/favorite', pet: @pet %>
        </div>
        <div class="comments_<%= @pet.id %>">
          <%= render 'comments/comment', pet: @pet, comment: @comment %>
        </div>
        <% end %>
        <br>
        <% if @pet.user == current_user %>
          <%= link_to "編集する", edit_pet_path(@pet.id),  class: "btn btn-secondary" %>
        <% end %>
      </div>
    </div>
  </div>
</div>